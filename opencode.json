{
  "$schema": "https://opencode.ai/config.json",
  "mcp": {
    "sqlite": {
      "type": "local",
      "command": ["py", "-c", "from mcp_server_sqlite.server import main; import asyncio; asyncio.run(main('app.db'))"],
      "enabled": true
    }
  }
}
